version: "2"

sql:
  - engine: "postgresql"
    queries: "./internal/repository/queries.sql"
    schema: "./internal/database/migrations"
    gen:
      go:
        package: "repository"
        sql_package: "pgx/v5"
        out: "./internal/repository"
        emit_db_tags: true
